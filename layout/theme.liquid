<!doctype html>
<html lang="{{ request.locale.iso_code }}" class="dark">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ page_title }} | Museum of Impossible Things</title>
    
    <!-- CSS Loading -->
    {{ 'neural-aurora.css' | asset_url | stylesheet_tag }}
    
    <!-- Alpine.js for interactions -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    
    {{ content_for_header }}
  </head>
  <body class="antialiased min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
    
    <main id="MainContent" role="main" class="container mx-auto px-4 sm:px-6 lg:px-8">
      {{ content_for_layout }}
    </main>

  </body>
</html>